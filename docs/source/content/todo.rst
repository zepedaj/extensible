
Wish List
-----------

.. todo::

    * Add a fixture that is printed as part of the TQDM description.
    * Add a button to each plot that maximizes it to cover the full screen
    * Add the ability to select curves from legends across various plots and assemble them into a single plot - otherwise the selected plots could be taken to be the plot's name with the legend name appended.
